I should beautify this, sometime later.
Currently waiting on output, to see if this works for a relatively large file of 1hr duration and size 3gb, shot in 1080p format.

To run tests, run python -m pytest